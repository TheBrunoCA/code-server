services:
  code-server:
    build: .

    restart: unless-stopped

    expose:
      - "8080"

    environment:
      PASSWORD: ${PASSWORD}
      TZ: ${TZ}

    volumes:
      - code-server-home:/home/coder

    user: "1000:1000"

volumes:
  code-server-home:
